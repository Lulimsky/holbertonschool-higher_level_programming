#!/usr/bin/python3
"""define class Square"""

class Square:

    def __init__(self, size):
        """Size instance o a square with
        no type either value verification, 
        given as a private attribute"""
        self.__size = size
